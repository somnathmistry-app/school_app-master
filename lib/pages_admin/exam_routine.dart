import 'package:flutter/material.dart';

class ExamRoutineUpload extends StatelessWidget {
  const ExamRoutineUpload({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.black87,
      body: Container(),
    );
  }
}
